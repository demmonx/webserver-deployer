
{NAME} ansible_ssh_host={IP} ansible_ssh_port=22

postfix__packages: [ 'libsasl2-modules' ]
postfix__lookup_tables:

  - name: 'smtp_sasl_password_maps.in'
    mode: '0600'
    options:

      - name: '[{SMTP_HOST}]'
        value: '{{ lookup("file", secret + "/postfix/smtp-auth.key") }}'